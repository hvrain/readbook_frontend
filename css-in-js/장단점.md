# CSS in JS 의 장단점

## 장점

- js 파일만 보면 되기 때문에, 모듈화에 유리하고, css를 유지보수할 필요가 없다.
- js 환경을 이용하기 때문에, 상수와 함수를 쉽게 공유할 수 있다.
- 중복되지 않는 클래스 이름을 직접 만들어 주기 때문에 중복을 걱정할 필요가 없다.
- 현재 사용중인 컴포넌트만 stylesheet에 포함된다.

## 단점

- 별도의 라이브러리를 사용하므로 번들의 크기가 커진다.
- css 파일을 사용하는 것에 비해 속도가 많이 느리다.